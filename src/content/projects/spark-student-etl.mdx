---
title: "Spark Student ETL"
description: "A PySpark pipeline that cleans student, assignment, and submission data and builds bronze, silver, and gold outputs."
date: 2026-04-06
featured: true
category: "Spark ETL"
status: "shipped"
challenge: "Multiple raw sources fed student submissions, assignments, and roster data into one ETL flow, but repeated submissions and join collisions made the curated layer hard to trust."
architecture: "Raw CSV inputs are loaded through notebook and CLI helpers, deduplicated by submission id, joined with explicit class ownership rules, and published into bronze, silver, and gold parquet tables."
scale: "Small sample data that still includes duplicate submissions, conflicting class ownership, and realistic aggregation needs for class and student summaries."
outcome: "A reproducible Spark ETL project that demonstrates deduplication, join hygiene, summary modeling, and notebook-based walkthroughs."
tags:
  - spark
  - etl
  - bronze-silver-gold
stack:
  - PySpark
  - Python
  - Parquet
  - Jupyter
  - pytest
lessons:
  - Deduplication should be defined by business keys and recency rules before anything gets summarized.
  - Joins need explicit column selection or duplicate fields will surface later as ambiguous group-by errors.
  - Notebook helpers make an ETL project easier to explain than a single opaque run command.
repo: "https://github.com/Nikhil5hetty/spark-student-etl.git"
diagram: "/diagrams/spark-student-etl.svg"
---

## Overview

This project is the cleanest Spark ETL example in the portfolio. It shows the full path from raw
student data to curated outputs, while keeping the implementation simple enough to inspect in a
notebook.

## Implementation details

- Notebook-local helper functions load raw CSV files, display intermediate results, and write the outputs.
- `dedupe_submissions()` keeps the latest submission for each `submission_id` using submitted time,
  attempt number, and score as the ordering keys.
- `build_enriched_submissions()` resolves `class_id` collisions explicitly so downstream aggregates
  are deterministic.
- The pipeline writes three layers:
  - bronze: deduplicated submissions
  - silver: enriched student/assignment submission fact table
  - gold: class and student summaries

## What the pipeline proves

- structured ingestion with PySpark
- deduplication based on real business rules
- safe joins with explicit column handling
- summary tables that support reporting and dashboards

## Why it matters

This is the kind of project that maps directly to a data engineering interview:
you can explain the raw input, the transformation rules, the output layers, and the reason each step
exists.
